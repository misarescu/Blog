---
const { imgSrc, title, preview } = Astro.props;
---

<div
  class="not-prose dark:bg-slate-600 bg-slate-100 md:shadow-xl shadow-md min-w-full flex flex-col items-center"
>
  <!-- <img src={imgSrc} class="lg:min-h-60 md:min-h-40 min-h-20" /> -->
  <div
    class="dark:bg-slate-950 bg-slate-50 lg:w-full lg:h-60 md:h-40 w-1/2 h-20 md:inline-block hidden"
  >
    img source: {imgSrc}
  </div>
  <div class="md:p-8 p-4">
    <h2 class="text-center lg:text-3xl md:text-2xl text-xl font-extrabold">
      {title}
    </h2>
    <p>{preview}...</p>
  </div>
</div>
